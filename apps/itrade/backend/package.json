{
  "name": "itrade-backend",
  "version": "0.1.0",
  "description": "iTrade backend",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "js:watch": "nodemon -e 'js' build/index.js",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts:watch": "tsc --watch"
  },
  "author": "Alphien",
  "dependencies": {
    "fastify": "^4.7.0"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3"
  }
}
