{
  "name": "irms-frontend",
  "description": "iRMS frontend",
  "main": "index.js",
  "scripts": {
    "build": "vite build --base ./",
    "dev": "vite --force",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist node_modules",
    "install": "sed -i 's/}(this));/}(this || self ||  globalThis));/g' ../../node_modules/jqwidgets-framework/jqwidgets/globalization/globalize.js "
  },
  "author": "Alphien",
  "dependencies": {
    "axios": "^1.1.2",
    "jqwidgets-framework": "^14.0.0",
    "vue": "~2.7.10"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.0.0",
    "sass": "^1.55.0",
    "vite": "^3.1.4"
  }
}
